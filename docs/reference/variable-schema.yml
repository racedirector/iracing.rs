$schema: https://json-schema.org/draft/2020-12/schema
title: Variable info
description: Information about a specific telemetry variable.
type: object
properties:
  count:
    title: Count
    description: Number of elements (1 for scalar, >1 for arrays)
    type: integer
    format: uint
    minimum: 0
  count_as_time:
    title: Count as time
    description: Whether the simulator treats the sample count as elapsed time
    type: boolean
  data_type:
    title: Data type
    description: Data type of the variable
    type: string
    enum:
    - Character
    - Boolean
    - Integer
    - BitField
    - Float
    - Double
  description:
    title: Description
    description: Human-readable description
    type: string
  name:
    title: Name
    description: Variable name as defined by iRacing
    type: string
  offset:
    title: Byte offset
    description: Byte offset within the telemetry frame
    type: integer
    format: uint
    minimum: 0
  units:
    title: Units
    description: Units of measurement (e.g., "m/s", "C", "N*m")
    type: string
required:
- name
- data_type
- offset
- count
- count_as_time
- units
- description
